<div class="max-w-7xl mx-auto px-4 py-10 grid grid-cols-1 md:grid-cols-3 gap-6">

  <div class="md:col-span-2">
    <div class="mb-6">
      <%= form_with url: admin_tags_path, method: :get, local: true, class: "flex gap-2" do |f| %>
        <%= f.text_field :q, value: params[:q], placeholder: t('.search_tag'), class: "w-full border border-gray-300 px-3 py-2 rounded-none shadow-sm focus:ring focus:ring-gray-500 focus:border-gray-700" %>
        <%= f.submit t('.search'), class: "px-4 py-2 bg-gray-800 text-white text-sm font-medium rounded-none hover:bg-gray-700 transition" %>
      <% end %>
    </div>

    <div class="grid gap-4 sm:grid-cols-2 md:grid-cols-2 lg:grid-cols-3">
      <% @tags.each do |tag| %>
        <div class="border-2 border-gray-700 bg-white p-4 shadow-sm">
          <p class="text-gray-800 font-semibold"><%= tag.name %></p>
        </div>
      <% end %>
    </div>

    <% if @tags.empty? %>
      <div class="mt-6 text-center text-gray-500">
        <p><%= t('.no_tags_found') %></p>
      </div>
    <% end %>
  </div>

  <div class="md:col-span-1 bg-white border-2 border-gray-700 p-6 shadow-sm">
    <h2 class="text-lg font-semibold text-gray-800 mb-4">
      <%= t('.new_tag') %>
    </h2>

    <%= form_with model: @tag, url: admin_tags_path do |f| %>
      <%= form_text_field(f, :name) %>
      <%= form_errors(@tag, :name) %>

      <%= form_submit_button(t('.save')) %>
    <% end %>
  </div>
</div>
