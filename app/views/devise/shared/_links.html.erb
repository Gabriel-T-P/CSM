<div class="flex flex-col items-center space-y-2 mt-4 text-center">
  <% if controller_name != 'sessions' %>
    <%= link_to t('.log_in'), new_session_path(resource_name),
      class: "border-b-2 border-gray-800 text-gray-800 font-medium hover:border-gray-400 hover:text-gray-500 transition" %>
  <% end %>

  <% if devise_mapping.registerable? && controller_name != 'registrations' %>
    <%= link_to t('.sign_up'), new_registration_path(resource_name),
      class: "border-b-2 border-gray-800 text-gray-800 font-medium hover:border-gray-400 hover:text-gray-500 transition" %>
  <% end %>

  <% if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
    <%= link_to t('.forgot_password'), new_password_path(resource_name),
      class: "border-b-2 border-gray-800 text-gray-800 font-medium hover:border-gray-400 hover:text-gray-500 transition" %>
  <% end %>

  <% if devise_mapping.confirmable? && controller_name != 'confirmations' %>
    <%= link_to t('.confirmation_instructions'), new_confirmation_path(resource_name),
      class: "border-b-2 border-gray-800 text-gray-800 font-medium hover:border-gray-400 hover:text-gray-500 transition" %>
  <% end %>

  <% if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
    <%= link_to t('.unlock_instructions'), new_unlock_path(resource_name),
      class: "border-b-2 border-gray-800 text-gray-800 font-medium hover:border-gray-400 hover:text-gray-500 transition" %>
  <% end %>

  <% if devise_mapping.omniauthable? %>
    <% resource_class.omniauth_providers.each do |provider| %>
      <%= button_to "Sign in with #{OmniAuth::Utils.camelize(provider)}",
          omniauth_authorize_path(resource_name, provider),
          data: { turbo: false },
          class: "border-b-2 border-gray-800 text-gray-800 font-medium hover:border-gray-400 hover:text-gray-500 transition" %>
    <% end %>
  <% end %>
</div>
